export function SectionSeparator() {
  return (
    <div
      className="w-full h-1 my-12 rounded-full"
      style={{
        background: 'linear-gradient(to right, transparent, var(--primary-color), transparent)'
      }}
    />
  )
}
